{
  "mcpServers": {
    "godot": {
      "command": "npx",
      "args": ["-y", "@coding-solo/godot-mcp"],
      "env": {
        "DEBUG": "true",
        "GODOT_PATH": "${GODOT_PATH}"
      }
    }
  }
}
